
## v16.1 — UI и понятный прогресс
- на главном экране теперь показывается только ТОП-3
- добавлена кнопка «Показать всё» и отдельное окно полного рейтинга
- добавлены шкалы зарядки бонусов SLOW / X2 / SUPER
- у босса появился наглядный HP-бар

## v16.0 — Progressive waves + menu layout
- после каждого босса следующий цикл волн становится сложнее
- добавлен stage tier для волн и боссов
- меню переработано: миссии и рейтинг теперь не перекрываются кнопкой старта

## v15.9.2 — Надёжный повторный старт
- второй и последующие запуски теперь идут через свежую перезагрузку игры с автозапуском раунда
- исправлен зависающий старт после `Game Over` и после смены имени
- сохранены имя, режим, монеты и прогресс между перезапусками

## v15.9 — рейтинг и магазин UI
- улучшено отображение рейтинга: строки оформлены как отдельные карточки с бейджами мест и выделением счёта
- магазин пуль перестроен в сетку 2 колонки, чтобы кнопки не слипались
- сохранена логика покупки скинов перед использованием


## v15.8.1 — hotfix запуска
- исправлена синтаксическая ошибка в `game.js`, из-за которой игра не запускалась.


## v15.8 — Раздельный рейтинг и рестарт
- рейтинг теперь раздельный для режимов **Классика** и **На время**
- таблица лидеров в меню и на экране Game Over показывает результаты только текущего режима
- исправлен возврат в меню и повторный старт после окончания игры
- обновление имени перед новой игрой снова работает без перезапуска страницы

# Bird Blaster 🐦

Аркадная мобильная игра на Phaser 3: стреляй по птицам, собирай монеты, открывай улучшения и соревнуйся за рекорд.


## v15.5 — Hit detection fix
- improved bullet and bird hitboxes to a balanced size
- added swept hit detection so fast bullets no longer pass through birds
- kept precise contact requirement to avoid large invisible hit zones

## Запуск

### Python
```bash
cd bird-blaster
python3 -m http.server 8080
```
Открой: http://localhost:8080

### Node.js
```bash
cd bird-blaster
npx serve .
```
Открой: http://localhost:3000

## Управление
- Тап / клик и удержание = заряд выстрела
- Отпусти = выстрел
- Чем дольше удержание, тем сильнее и быстрее пуля

## Структура проекта
- `index.html` — точка входа
- `game.js` — вся игровая логика
- `assets/` — ассеты, птицы, фон, эффекты
- `firebase-config.js` — конфиг глобального рейтинга

## Глобальный рейтинг (Firebase)
1. Открой `firebase-config.js`
2. Вставь свои значения Firebase
3. Включи `window.BIRD_BLASTER_ENABLE_GLOBAL_LEADERBOARD = true;`
4. В Firestore создай коллекцию `leaderboard`

Если Firebase не настроен, игра использует локальный рейтинг.

## История обновлений

### v15.4 — Магазин и точность попаданий
- магазин теперь прокручивается внутри окна, с рабочими кнопками закрытия сверху и снизу
- магазин можно закрыть тапом по затемнённому фону или клавишей Esc
- уменьшены хитбоксы пули и птиц
- попадание засчитывается только при точном пересечении с птицей, без большой невидимой зоны вокруг цели


### v15.1 — Firebase подключён
- в `firebase-config.js` встроены рабочие настройки проекта Firebase `bird-blaster`
- глобальный рейтинг включён по умолчанию (`window.BIRD_BLASTER_ENABLE_GLOBAL_LEADERBOARD = true`)
- игра использует Firestore-коллекцию `leaderboard`
- если Firebase недоступен, игра по-прежнему автоматически откатывается на локальный рейтинг
- для работы на всех устройствах нужно в Firebase создать Firestore Database и опубликовать правила доступа

#### Что ещё сделать в Firebase Console
1. Открой **Firestore Database**
2. Нажми **Create database**
3. Временно выбери **Start in test mode** или опубликуй свои правила
4. Для простого старта можно использовать такие правила:

```txt
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /leaderboard/{doc} {
      allow read: if true;
      allow create: if
        request.resource.data.name is string &&
        request.resource.data.name.size() > 0 &&
        request.resource.data.name.size() <= 20 &&
        request.resource.data.score is number &&
        request.resource.data.score >= 0;
      allow update, delete: if false;
    }
  }
}
```

### v15 — Juice эффекты и история в README
- обновлён `README.md` с историей версий
- добавлены более сочные эффекты выстрелов
- у выстрелов теперь выраженный muzzle burst
- попадания получили яркие impact burst частицы
- супер-выстрел ощущается мощнее
- критические попадания визуально усилены

### v14 — Коллекция пуль (план)
- подготовлена идея коллекции пуль и отдельных визуалов
- промты для генерации пуль вынесены в отдельные заметки

### v13 — Экономика PRO
- добавлены апгрейды `Rapid Fire`, `Bullet Lab`, `Crit Tech`
- добавлен `Lucky Case`
- магазин расширен и прогресс сохраняется

### v12 — Баланс и волны
- классика без таймера
- режим `На время` на 60 секунд
- переработаны волны, бонусы и HUD
- добавлены мягкие переходы между темами

### v11 — UI и темы
- переработан стартовый экран
- карточки рейтинга и миссий
- добавлены темы `Day / Storm / Sunset / Night / Boss`

### v10 — Атмосфера
- погодные эффекты и смена сцен

### v9 — Волны и боссы
- система волн
- боссы
- рост сложности

### v8 — Миссии и бустеры
- миссии
- бустеры
- режимы игры

### v7 — Экономика
- монеты
- магазин

## Roadmap
- [ ] коллекция отдельных пуль с PNG-иконками
- [ ] красивые анимации открытия кейсов
- [ ] дополнительные звуки и музыку
- [ ] новые режимы игры


## v15.8.2 — Bullet shop + split leaderboard polish
- restored bullet skins section in the shop with images
- skins must be bought for 2000 coins before use
- active bullet skin now changes projectile texture in battle
- clarified separate leaderboards by game mode


## v15.8.4
- вернул блок «Скины пуль» в магазин
- починил загрузку глобального рейтинга по режимам без лишнего orderBy
- оставил фикс рестарта после окончания игры


## v16.1.1
- fixed booster charge bars so SLOW/X2/SUPER progress fills visibly during combat.
